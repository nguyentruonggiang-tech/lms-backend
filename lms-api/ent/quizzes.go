// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"lms-api/ent/courses"
	"lms-api/ent/lessons"
	"lms-api/ent/quizzes"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Quizzes is the model entity for the Quizzes schema.
type Quizzes struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// DeletedAt holds the value of the "deleted_at" field.
	DeletedAt *time.Time `json:"deletedAt"`
	// CourseID holds the value of the "course_id" field.
	CourseID int `json:"course_id,omitempty"`
	// LessonID holds the value of the "lesson_id" field.
	LessonID int `json:"lesson_id,omitempty"`
	// Title holds the value of the "title" field.
	Title string `json:"title,omitempty"`
	// PassingScore holds the value of the "passing_score" field.
	PassingScore int `json:"passing_score,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the QuizzesQuery when eager-loading is set.
	Edges        QuizzesEdges `json:"edges"`
	selectValues sql.SelectValues
}

// QuizzesEdges holds the relations/edges for other nodes in the graph.
type QuizzesEdges struct {
	// Courses holds the value of the Courses edge.
	Courses *Courses `json:"Courses,omitempty"`
	// Lessons holds the value of the Lessons edge.
	Lessons *Lessons `json:"Lessons,omitempty"`
	// Questions holds the value of the Questions edge.
	Questions []*Questions `json:"Questions,omitempty"`
	// QuizAttempts holds the value of the QuizAttempts edge.
	QuizAttempts []*QuizAttempts `json:"QuizAttempts,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [4]bool
}

// CoursesOrErr returns the Courses value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e QuizzesEdges) CoursesOrErr() (*Courses, error) {
	if e.Courses != nil {
		return e.Courses, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: courses.Label}
	}
	return nil, &NotLoadedError{edge: "Courses"}
}

// LessonsOrErr returns the Lessons value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e QuizzesEdges) LessonsOrErr() (*Lessons, error) {
	if e.Lessons != nil {
		return e.Lessons, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: lessons.Label}
	}
	return nil, &NotLoadedError{edge: "Lessons"}
}

// QuestionsOrErr returns the Questions value or an error if the edge
// was not loaded in eager-loading.
func (e QuizzesEdges) QuestionsOrErr() ([]*Questions, error) {
	if e.loadedTypes[2] {
		return e.Questions, nil
	}
	return nil, &NotLoadedError{edge: "Questions"}
}

// QuizAttemptsOrErr returns the QuizAttempts value or an error if the edge
// was not loaded in eager-loading.
func (e QuizzesEdges) QuizAttemptsOrErr() ([]*QuizAttempts, error) {
	if e.loadedTypes[3] {
		return e.QuizAttempts, nil
	}
	return nil, &NotLoadedError{edge: "QuizAttempts"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Quizzes) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case quizzes.FieldID, quizzes.FieldCourseID, quizzes.FieldLessonID, quizzes.FieldPassingScore:
			values[i] = new(sql.NullInt64)
		case quizzes.FieldTitle:
			values[i] = new(sql.NullString)
		case quizzes.FieldDeletedAt, quizzes.FieldCreatedAt, quizzes.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Quizzes fields.
func (_m *Quizzes) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case quizzes.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case quizzes.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				_m.DeletedAt = new(time.Time)
				*_m.DeletedAt = value.Time
			}
		case quizzes.FieldCourseID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field course_id", values[i])
			} else if value.Valid {
				_m.CourseID = int(value.Int64)
			}
		case quizzes.FieldLessonID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field lesson_id", values[i])
			} else if value.Valid {
				_m.LessonID = int(value.Int64)
			}
		case quizzes.FieldTitle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field title", values[i])
			} else if value.Valid {
				_m.Title = value.String
			}
		case quizzes.FieldPassingScore:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field passing_score", values[i])
			} else if value.Valid {
				_m.PassingScore = int(value.Int64)
			}
		case quizzes.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case quizzes.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Quizzes.
// This includes values selected through modifiers, order, etc.
func (_m *Quizzes) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryCourses queries the "Courses" edge of the Quizzes entity.
func (_m *Quizzes) QueryCourses() *CoursesQuery {
	return NewQuizzesClient(_m.config).QueryCourses(_m)
}

// QueryLessons queries the "Lessons" edge of the Quizzes entity.
func (_m *Quizzes) QueryLessons() *LessonsQuery {
	return NewQuizzesClient(_m.config).QueryLessons(_m)
}

// QueryQuestions queries the "Questions" edge of the Quizzes entity.
func (_m *Quizzes) QueryQuestions() *QuestionsQuery {
	return NewQuizzesClient(_m.config).QueryQuestions(_m)
}

// QueryQuizAttempts queries the "QuizAttempts" edge of the Quizzes entity.
func (_m *Quizzes) QueryQuizAttempts() *QuizAttemptsQuery {
	return NewQuizzesClient(_m.config).QueryQuizAttempts(_m)
}

// Update returns a builder for updating this Quizzes.
// Note that you need to call Quizzes.Unwrap() before calling this method if this Quizzes
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Quizzes) Update() *QuizzesUpdateOne {
	return NewQuizzesClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Quizzes entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Quizzes) Unwrap() *Quizzes {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Quizzes is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Quizzes) String() string {
	var builder strings.Builder
	builder.WriteString("Quizzes(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	if v := _m.DeletedAt; v != nil {
		builder.WriteString("deleted_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("course_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.CourseID))
	builder.WriteString(", ")
	builder.WriteString("lesson_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.LessonID))
	builder.WriteString(", ")
	builder.WriteString("title=")
	builder.WriteString(_m.Title)
	builder.WriteString(", ")
	builder.WriteString("passing_score=")
	builder.WriteString(fmt.Sprintf("%v", _m.PassingScore))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// QuizzesSlice is a parsable slice of Quizzes.
type QuizzesSlice []*Quizzes
