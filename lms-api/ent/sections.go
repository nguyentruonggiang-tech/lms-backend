// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"lms-api/ent/courses"
	"lms-api/ent/sections"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Sections is the model entity for the Sections schema.
type Sections struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// DeletedAt holds the value of the "deleted_at" field.
	DeletedAt *time.Time `json:"deletedAt"`
	// CourseID holds the value of the "course_id" field.
	CourseID int `json:"course_id,omitempty"`
	// Title holds the value of the "title" field.
	Title string `json:"title,omitempty"`
	// SortOrder holds the value of the "sort_order" field.
	SortOrder int `json:"sort_order,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SectionsQuery when eager-loading is set.
	Edges        SectionsEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SectionsEdges holds the relations/edges for other nodes in the graph.
type SectionsEdges struct {
	// Courses holds the value of the Courses edge.
	Courses *Courses `json:"Courses,omitempty"`
	// Lessons holds the value of the Lessons edge.
	Lessons []*Lessons `json:"Lessons,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// CoursesOrErr returns the Courses value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SectionsEdges) CoursesOrErr() (*Courses, error) {
	if e.Courses != nil {
		return e.Courses, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: courses.Label}
	}
	return nil, &NotLoadedError{edge: "Courses"}
}

// LessonsOrErr returns the Lessons value or an error if the edge
// was not loaded in eager-loading.
func (e SectionsEdges) LessonsOrErr() ([]*Lessons, error) {
	if e.loadedTypes[1] {
		return e.Lessons, nil
	}
	return nil, &NotLoadedError{edge: "Lessons"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Sections) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case sections.FieldID, sections.FieldCourseID, sections.FieldSortOrder:
			values[i] = new(sql.NullInt64)
		case sections.FieldTitle:
			values[i] = new(sql.NullString)
		case sections.FieldDeletedAt, sections.FieldCreatedAt, sections.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Sections fields.
func (_m *Sections) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case sections.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case sections.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				_m.DeletedAt = new(time.Time)
				*_m.DeletedAt = value.Time
			}
		case sections.FieldCourseID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field course_id", values[i])
			} else if value.Valid {
				_m.CourseID = int(value.Int64)
			}
		case sections.FieldTitle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field title", values[i])
			} else if value.Valid {
				_m.Title = value.String
			}
		case sections.FieldSortOrder:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field sort_order", values[i])
			} else if value.Valid {
				_m.SortOrder = int(value.Int64)
			}
		case sections.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case sections.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Sections.
// This includes values selected through modifiers, order, etc.
func (_m *Sections) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryCourses queries the "Courses" edge of the Sections entity.
func (_m *Sections) QueryCourses() *CoursesQuery {
	return NewSectionsClient(_m.config).QueryCourses(_m)
}

// QueryLessons queries the "Lessons" edge of the Sections entity.
func (_m *Sections) QueryLessons() *LessonsQuery {
	return NewSectionsClient(_m.config).QueryLessons(_m)
}

// Update returns a builder for updating this Sections.
// Note that you need to call Sections.Unwrap() before calling this method if this Sections
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Sections) Update() *SectionsUpdateOne {
	return NewSectionsClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Sections entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Sections) Unwrap() *Sections {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Sections is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Sections) String() string {
	var builder strings.Builder
	builder.WriteString("Sections(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	if v := _m.DeletedAt; v != nil {
		builder.WriteString("deleted_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	builder.WriteString("course_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.CourseID))
	builder.WriteString(", ")
	builder.WriteString("title=")
	builder.WriteString(_m.Title)
	builder.WriteString(", ")
	builder.WriteString("sort_order=")
	builder.WriteString(fmt.Sprintf("%v", _m.SortOrder))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SectionsSlice is a parsable slice of Sections.
type SectionsSlice []*Sections
