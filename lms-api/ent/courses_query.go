// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"lms-api/ent/categories"
	"lms-api/ent/certificates"
	"lms-api/ent/courses"
	"lms-api/ent/enrollments"
	"lms-api/ent/lessonprogresses"
	"lms-api/ent/lessons"
	"lms-api/ent/predicate"
	"lms-api/ent/quizzes"
	"lms-api/ent/sections"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// CoursesQuery is the builder for querying Courses entities.
type CoursesQuery struct {
	config
	ctx                  *QueryContext
	order                []courses.OrderOption
	inters               []Interceptor
	predicates           []predicate.Courses
	withCategories       *CategoriesQuery
	withSections         *SectionsQuery
	withLessons          *LessonsQuery
	withQuizzes          *QuizzesQuery
	withEnrollments      *EnrollmentsQuery
	withLessonProgresses *LessonProgressesQuery
	withCertificates     *CertificatesQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CoursesQuery builder.
func (_q *CoursesQuery) Where(ps ...predicate.Courses) *CoursesQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *CoursesQuery) Limit(limit int) *CoursesQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *CoursesQuery) Offset(offset int) *CoursesQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *CoursesQuery) Unique(unique bool) *CoursesQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *CoursesQuery) Order(o ...courses.OrderOption) *CoursesQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryCategories chains the current query on the "Categories" edge.
func (_q *CoursesQuery) QueryCategories() *CategoriesQuery {
	query := (&CategoriesClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(courses.Table, courses.FieldID, selector),
			sqlgraph.To(categories.Table, categories.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, courses.CategoriesTable, courses.CategoriesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySections chains the current query on the "Sections" edge.
func (_q *CoursesQuery) QuerySections() *SectionsQuery {
	query := (&SectionsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(courses.Table, courses.FieldID, selector),
			sqlgraph.To(sections.Table, sections.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, courses.SectionsTable, courses.SectionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryLessons chains the current query on the "Lessons" edge.
func (_q *CoursesQuery) QueryLessons() *LessonsQuery {
	query := (&LessonsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(courses.Table, courses.FieldID, selector),
			sqlgraph.To(lessons.Table, lessons.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, courses.LessonsTable, courses.LessonsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryQuizzes chains the current query on the "Quizzes" edge.
func (_q *CoursesQuery) QueryQuizzes() *QuizzesQuery {
	query := (&QuizzesClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(courses.Table, courses.FieldID, selector),
			sqlgraph.To(quizzes.Table, quizzes.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, courses.QuizzesTable, courses.QuizzesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryEnrollments chains the current query on the "Enrollments" edge.
func (_q *CoursesQuery) QueryEnrollments() *EnrollmentsQuery {
	query := (&EnrollmentsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(courses.Table, courses.FieldID, selector),
			sqlgraph.To(enrollments.Table, enrollments.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, courses.EnrollmentsTable, courses.EnrollmentsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryLessonProgresses chains the current query on the "LessonProgresses" edge.
func (_q *CoursesQuery) QueryLessonProgresses() *LessonProgressesQuery {
	query := (&LessonProgressesClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(courses.Table, courses.FieldID, selector),
			sqlgraph.To(lessonprogresses.Table, lessonprogresses.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, courses.LessonProgressesTable, courses.LessonProgressesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCertificates chains the current query on the "Certificates" edge.
func (_q *CoursesQuery) QueryCertificates() *CertificatesQuery {
	query := (&CertificatesClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(courses.Table, courses.FieldID, selector),
			sqlgraph.To(certificates.Table, certificates.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, courses.CertificatesTable, courses.CertificatesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Courses entity from the query.
// Returns a *NotFoundError when no Courses was found.
func (_q *CoursesQuery) First(ctx context.Context) (*Courses, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{courses.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *CoursesQuery) FirstX(ctx context.Context) *Courses {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Courses ID from the query.
// Returns a *NotFoundError when no Courses ID was found.
func (_q *CoursesQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{courses.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *CoursesQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Courses entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Courses entity is found.
// Returns a *NotFoundError when no Courses entities are found.
func (_q *CoursesQuery) Only(ctx context.Context) (*Courses, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{courses.Label}
	default:
		return nil, &NotSingularError{courses.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *CoursesQuery) OnlyX(ctx context.Context) *Courses {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Courses ID in the query.
// Returns a *NotSingularError when more than one Courses ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *CoursesQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{courses.Label}
	default:
		err = &NotSingularError{courses.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *CoursesQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CoursesSlice.
func (_q *CoursesQuery) All(ctx context.Context) ([]*Courses, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Courses, *CoursesQuery]()
	return withInterceptors[[]*Courses](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *CoursesQuery) AllX(ctx context.Context) []*Courses {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Courses IDs.
func (_q *CoursesQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(courses.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *CoursesQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *CoursesQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*CoursesQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *CoursesQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *CoursesQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *CoursesQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CoursesQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *CoursesQuery) Clone() *CoursesQuery {
	if _q == nil {
		return nil
	}
	return &CoursesQuery{
		config:               _q.config,
		ctx:                  _q.ctx.Clone(),
		order:                append([]courses.OrderOption{}, _q.order...),
		inters:               append([]Interceptor{}, _q.inters...),
		predicates:           append([]predicate.Courses{}, _q.predicates...),
		withCategories:       _q.withCategories.Clone(),
		withSections:         _q.withSections.Clone(),
		withLessons:          _q.withLessons.Clone(),
		withQuizzes:          _q.withQuizzes.Clone(),
		withEnrollments:      _q.withEnrollments.Clone(),
		withLessonProgresses: _q.withLessonProgresses.Clone(),
		withCertificates:     _q.withCertificates.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithCategories tells the query-builder to eager-load the nodes that are connected to
// the "Categories" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CoursesQuery) WithCategories(opts ...func(*CategoriesQuery)) *CoursesQuery {
	query := (&CategoriesClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCategories = query
	return _q
}

// WithSections tells the query-builder to eager-load the nodes that are connected to
// the "Sections" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CoursesQuery) WithSections(opts ...func(*SectionsQuery)) *CoursesQuery {
	query := (&SectionsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSections = query
	return _q
}

// WithLessons tells the query-builder to eager-load the nodes that are connected to
// the "Lessons" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CoursesQuery) WithLessons(opts ...func(*LessonsQuery)) *CoursesQuery {
	query := (&LessonsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withLessons = query
	return _q
}

// WithQuizzes tells the query-builder to eager-load the nodes that are connected to
// the "Quizzes" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CoursesQuery) WithQuizzes(opts ...func(*QuizzesQuery)) *CoursesQuery {
	query := (&QuizzesClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withQuizzes = query
	return _q
}

// WithEnrollments tells the query-builder to eager-load the nodes that are connected to
// the "Enrollments" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CoursesQuery) WithEnrollments(opts ...func(*EnrollmentsQuery)) *CoursesQuery {
	query := (&EnrollmentsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withEnrollments = query
	return _q
}

// WithLessonProgresses tells the query-builder to eager-load the nodes that are connected to
// the "LessonProgresses" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CoursesQuery) WithLessonProgresses(opts ...func(*LessonProgressesQuery)) *CoursesQuery {
	query := (&LessonProgressesClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withLessonProgresses = query
	return _q
}

// WithCertificates tells the query-builder to eager-load the nodes that are connected to
// the "Certificates" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CoursesQuery) WithCertificates(opts ...func(*CertificatesQuery)) *CoursesQuery {
	query := (&CertificatesClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCertificates = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		DeletedAt time.Time `json:"deletedAt"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Courses.Query().
//		GroupBy(courses.FieldDeletedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *CoursesQuery) GroupBy(field string, fields ...string) *CoursesGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CoursesGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = courses.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		DeletedAt time.Time `json:"deletedAt"`
//	}
//
//	client.Courses.Query().
//		Select(courses.FieldDeletedAt).
//		Scan(ctx, &v)
func (_q *CoursesQuery) Select(fields ...string) *CoursesSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &CoursesSelect{CoursesQuery: _q}
	sbuild.label = courses.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CoursesSelect configured with the given aggregations.
func (_q *CoursesQuery) Aggregate(fns ...AggregateFunc) *CoursesSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *CoursesQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !courses.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *CoursesQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Courses, error) {
	var (
		nodes       = []*Courses{}
		_spec       = _q.querySpec()
		loadedTypes = [7]bool{
			_q.withCategories != nil,
			_q.withSections != nil,
			_q.withLessons != nil,
			_q.withQuizzes != nil,
			_q.withEnrollments != nil,
			_q.withLessonProgresses != nil,
			_q.withCertificates != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Courses).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Courses{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withCategories; query != nil {
		if err := _q.loadCategories(ctx, query, nodes, nil,
			func(n *Courses, e *Categories) { n.Edges.Categories = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withSections; query != nil {
		if err := _q.loadSections(ctx, query, nodes,
			func(n *Courses) { n.Edges.Sections = []*Sections{} },
			func(n *Courses, e *Sections) { n.Edges.Sections = append(n.Edges.Sections, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withLessons; query != nil {
		if err := _q.loadLessons(ctx, query, nodes,
			func(n *Courses) { n.Edges.Lessons = []*Lessons{} },
			func(n *Courses, e *Lessons) { n.Edges.Lessons = append(n.Edges.Lessons, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withQuizzes; query != nil {
		if err := _q.loadQuizzes(ctx, query, nodes,
			func(n *Courses) { n.Edges.Quizzes = []*Quizzes{} },
			func(n *Courses, e *Quizzes) { n.Edges.Quizzes = append(n.Edges.Quizzes, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withEnrollments; query != nil {
		if err := _q.loadEnrollments(ctx, query, nodes,
			func(n *Courses) { n.Edges.Enrollments = []*Enrollments{} },
			func(n *Courses, e *Enrollments) { n.Edges.Enrollments = append(n.Edges.Enrollments, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withLessonProgresses; query != nil {
		if err := _q.loadLessonProgresses(ctx, query, nodes,
			func(n *Courses) { n.Edges.LessonProgresses = []*LessonProgresses{} },
			func(n *Courses, e *LessonProgresses) { n.Edges.LessonProgresses = append(n.Edges.LessonProgresses, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withCertificates; query != nil {
		if err := _q.loadCertificates(ctx, query, nodes,
			func(n *Courses) { n.Edges.Certificates = []*Certificates{} },
			func(n *Courses, e *Certificates) { n.Edges.Certificates = append(n.Edges.Certificates, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *CoursesQuery) loadCategories(ctx context.Context, query *CategoriesQuery, nodes []*Courses, init func(*Courses), assign func(*Courses, *Categories)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Courses)
	for i := range nodes {
		fk := nodes[i].CategoryID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(categories.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "category_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *CoursesQuery) loadSections(ctx context.Context, query *SectionsQuery, nodes []*Courses, init func(*Courses), assign func(*Courses, *Sections)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Courses)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(sections.FieldCourseID)
	}
	query.Where(predicate.Sections(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(courses.SectionsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CourseID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "course_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CoursesQuery) loadLessons(ctx context.Context, query *LessonsQuery, nodes []*Courses, init func(*Courses), assign func(*Courses, *Lessons)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Courses)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(lessons.FieldCourseID)
	}
	query.Where(predicate.Lessons(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(courses.LessonsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CourseID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "course_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CoursesQuery) loadQuizzes(ctx context.Context, query *QuizzesQuery, nodes []*Courses, init func(*Courses), assign func(*Courses, *Quizzes)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Courses)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(quizzes.FieldCourseID)
	}
	query.Where(predicate.Quizzes(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(courses.QuizzesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CourseID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "course_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CoursesQuery) loadEnrollments(ctx context.Context, query *EnrollmentsQuery, nodes []*Courses, init func(*Courses), assign func(*Courses, *Enrollments)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Courses)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(enrollments.FieldCourseID)
	}
	query.Where(predicate.Enrollments(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(courses.EnrollmentsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CourseID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "course_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CoursesQuery) loadLessonProgresses(ctx context.Context, query *LessonProgressesQuery, nodes []*Courses, init func(*Courses), assign func(*Courses, *LessonProgresses)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Courses)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(lessonprogresses.FieldCourseID)
	}
	query.Where(predicate.LessonProgresses(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(courses.LessonProgressesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CourseID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "course_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CoursesQuery) loadCertificates(ctx context.Context, query *CertificatesQuery, nodes []*Courses, init func(*Courses), assign func(*Courses, *Certificates)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Courses)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(certificates.FieldCourseID)
	}
	query.Where(predicate.Certificates(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(courses.CertificatesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CourseID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "course_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *CoursesQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *CoursesQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(courses.Table, courses.Columns, sqlgraph.NewFieldSpec(courses.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, courses.FieldID)
		for i := range fields {
			if fields[i] != courses.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withCategories != nil {
			_spec.Node.AddColumnOnce(courses.FieldCategoryID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *CoursesQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(courses.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = courses.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CoursesGroupBy is the group-by builder for Courses entities.
type CoursesGroupBy struct {
	selector
	build *CoursesQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *CoursesGroupBy) Aggregate(fns ...AggregateFunc) *CoursesGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *CoursesGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CoursesQuery, *CoursesGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *CoursesGroupBy) sqlScan(ctx context.Context, root *CoursesQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CoursesSelect is the builder for selecting fields of Courses entities.
type CoursesSelect struct {
	*CoursesQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *CoursesSelect) Aggregate(fns ...AggregateFunc) *CoursesSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *CoursesSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CoursesQuery, *CoursesSelect](ctx, _s.CoursesQuery, _s, _s.inters, v)
}

func (_s *CoursesSelect) sqlScan(ctx context.Context, root *CoursesQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
