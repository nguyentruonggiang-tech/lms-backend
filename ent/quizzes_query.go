// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"lms-backend/ent/courses"
	"lms-backend/ent/lessons"
	"lms-backend/ent/predicate"
	"lms-backend/ent/questions"
	"lms-backend/ent/quizattempts"
	"lms-backend/ent/quizzes"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// QuizzesQuery is the builder for querying Quizzes entities.
type QuizzesQuery struct {
	config
	ctx              *QueryContext
	order            []quizzes.OrderOption
	inters           []Interceptor
	predicates       []predicate.Quizzes
	withCourses      *CoursesQuery
	withLessons      *LessonsQuery
	withQuestions    *QuestionsQuery
	withQuizAttempts *QuizAttemptsQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the QuizzesQuery builder.
func (_q *QuizzesQuery) Where(ps ...predicate.Quizzes) *QuizzesQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *QuizzesQuery) Limit(limit int) *QuizzesQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *QuizzesQuery) Offset(offset int) *QuizzesQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *QuizzesQuery) Unique(unique bool) *QuizzesQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *QuizzesQuery) Order(o ...quizzes.OrderOption) *QuizzesQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryCourses chains the current query on the "Courses" edge.
func (_q *QuizzesQuery) QueryCourses() *CoursesQuery {
	query := (&CoursesClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(quizzes.Table, quizzes.FieldID, selector),
			sqlgraph.To(courses.Table, courses.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, quizzes.CoursesTable, quizzes.CoursesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryLessons chains the current query on the "Lessons" edge.
func (_q *QuizzesQuery) QueryLessons() *LessonsQuery {
	query := (&LessonsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(quizzes.Table, quizzes.FieldID, selector),
			sqlgraph.To(lessons.Table, lessons.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, quizzes.LessonsTable, quizzes.LessonsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryQuestions chains the current query on the "Questions" edge.
func (_q *QuizzesQuery) QueryQuestions() *QuestionsQuery {
	query := (&QuestionsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(quizzes.Table, quizzes.FieldID, selector),
			sqlgraph.To(questions.Table, questions.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, quizzes.QuestionsTable, quizzes.QuestionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryQuizAttempts chains the current query on the "QuizAttempts" edge.
func (_q *QuizzesQuery) QueryQuizAttempts() *QuizAttemptsQuery {
	query := (&QuizAttemptsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(quizzes.Table, quizzes.FieldID, selector),
			sqlgraph.To(quizattempts.Table, quizattempts.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, quizzes.QuizAttemptsTable, quizzes.QuizAttemptsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Quizzes entity from the query.
// Returns a *NotFoundError when no Quizzes was found.
func (_q *QuizzesQuery) First(ctx context.Context) (*Quizzes, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{quizzes.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *QuizzesQuery) FirstX(ctx context.Context) *Quizzes {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Quizzes ID from the query.
// Returns a *NotFoundError when no Quizzes ID was found.
func (_q *QuizzesQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{quizzes.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *QuizzesQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Quizzes entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Quizzes entity is found.
// Returns a *NotFoundError when no Quizzes entities are found.
func (_q *QuizzesQuery) Only(ctx context.Context) (*Quizzes, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{quizzes.Label}
	default:
		return nil, &NotSingularError{quizzes.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *QuizzesQuery) OnlyX(ctx context.Context) *Quizzes {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Quizzes ID in the query.
// Returns a *NotSingularError when more than one Quizzes ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *QuizzesQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{quizzes.Label}
	default:
		err = &NotSingularError{quizzes.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *QuizzesQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of QuizzesSlice.
func (_q *QuizzesQuery) All(ctx context.Context) ([]*Quizzes, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Quizzes, *QuizzesQuery]()
	return withInterceptors[[]*Quizzes](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *QuizzesQuery) AllX(ctx context.Context) []*Quizzes {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Quizzes IDs.
func (_q *QuizzesQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(quizzes.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *QuizzesQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *QuizzesQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*QuizzesQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *QuizzesQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *QuizzesQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *QuizzesQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the QuizzesQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *QuizzesQuery) Clone() *QuizzesQuery {
	if _q == nil {
		return nil
	}
	return &QuizzesQuery{
		config:           _q.config,
		ctx:              _q.ctx.Clone(),
		order:            append([]quizzes.OrderOption{}, _q.order...),
		inters:           append([]Interceptor{}, _q.inters...),
		predicates:       append([]predicate.Quizzes{}, _q.predicates...),
		withCourses:      _q.withCourses.Clone(),
		withLessons:      _q.withLessons.Clone(),
		withQuestions:    _q.withQuestions.Clone(),
		withQuizAttempts: _q.withQuizAttempts.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithCourses tells the query-builder to eager-load the nodes that are connected to
// the "Courses" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *QuizzesQuery) WithCourses(opts ...func(*CoursesQuery)) *QuizzesQuery {
	query := (&CoursesClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withCourses = query
	return _q
}

// WithLessons tells the query-builder to eager-load the nodes that are connected to
// the "Lessons" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *QuizzesQuery) WithLessons(opts ...func(*LessonsQuery)) *QuizzesQuery {
	query := (&LessonsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withLessons = query
	return _q
}

// WithQuestions tells the query-builder to eager-load the nodes that are connected to
// the "Questions" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *QuizzesQuery) WithQuestions(opts ...func(*QuestionsQuery)) *QuizzesQuery {
	query := (&QuestionsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withQuestions = query
	return _q
}

// WithQuizAttempts tells the query-builder to eager-load the nodes that are connected to
// the "QuizAttempts" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *QuizzesQuery) WithQuizAttempts(opts ...func(*QuizAttemptsQuery)) *QuizzesQuery {
	query := (&QuizAttemptsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withQuizAttempts = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CourseID int `json:"course_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Quizzes.Query().
//		GroupBy(quizzes.FieldCourseID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *QuizzesQuery) GroupBy(field string, fields ...string) *QuizzesGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &QuizzesGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = quizzes.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CourseID int `json:"course_id,omitempty"`
//	}
//
//	client.Quizzes.Query().
//		Select(quizzes.FieldCourseID).
//		Scan(ctx, &v)
func (_q *QuizzesQuery) Select(fields ...string) *QuizzesSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &QuizzesSelect{QuizzesQuery: _q}
	sbuild.label = quizzes.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a QuizzesSelect configured with the given aggregations.
func (_q *QuizzesQuery) Aggregate(fns ...AggregateFunc) *QuizzesSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *QuizzesQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !quizzes.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *QuizzesQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Quizzes, error) {
	var (
		nodes       = []*Quizzes{}
		_spec       = _q.querySpec()
		loadedTypes = [4]bool{
			_q.withCourses != nil,
			_q.withLessons != nil,
			_q.withQuestions != nil,
			_q.withQuizAttempts != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Quizzes).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Quizzes{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withCourses; query != nil {
		if err := _q.loadCourses(ctx, query, nodes, nil,
			func(n *Quizzes, e *Courses) { n.Edges.Courses = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withLessons; query != nil {
		if err := _q.loadLessons(ctx, query, nodes, nil,
			func(n *Quizzes, e *Lessons) { n.Edges.Lessons = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withQuestions; query != nil {
		if err := _q.loadQuestions(ctx, query, nodes,
			func(n *Quizzes) { n.Edges.Questions = []*Questions{} },
			func(n *Quizzes, e *Questions) { n.Edges.Questions = append(n.Edges.Questions, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withQuizAttempts; query != nil {
		if err := _q.loadQuizAttempts(ctx, query, nodes,
			func(n *Quizzes) { n.Edges.QuizAttempts = []*QuizAttempts{} },
			func(n *Quizzes, e *QuizAttempts) { n.Edges.QuizAttempts = append(n.Edges.QuizAttempts, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *QuizzesQuery) loadCourses(ctx context.Context, query *CoursesQuery, nodes []*Quizzes, init func(*Quizzes), assign func(*Quizzes, *Courses)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Quizzes)
	for i := range nodes {
		fk := nodes[i].CourseID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(courses.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "course_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *QuizzesQuery) loadLessons(ctx context.Context, query *LessonsQuery, nodes []*Quizzes, init func(*Quizzes), assign func(*Quizzes, *Lessons)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*Quizzes)
	for i := range nodes {
		fk := nodes[i].LessonID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(lessons.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "lesson_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *QuizzesQuery) loadQuestions(ctx context.Context, query *QuestionsQuery, nodes []*Quizzes, init func(*Quizzes), assign func(*Quizzes, *Questions)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Quizzes)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(questions.FieldQuizID)
	}
	query.Where(predicate.Questions(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(quizzes.QuestionsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.QuizID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "quiz_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *QuizzesQuery) loadQuizAttempts(ctx context.Context, query *QuizAttemptsQuery, nodes []*Quizzes, init func(*Quizzes), assign func(*Quizzes, *QuizAttempts)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Quizzes)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(quizattempts.FieldQuizID)
	}
	query.Where(predicate.QuizAttempts(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(quizzes.QuizAttemptsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.QuizID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "quiz_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *QuizzesQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *QuizzesQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(quizzes.Table, quizzes.Columns, sqlgraph.NewFieldSpec(quizzes.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, quizzes.FieldID)
		for i := range fields {
			if fields[i] != quizzes.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withCourses != nil {
			_spec.Node.AddColumnOnce(quizzes.FieldCourseID)
		}
		if _q.withLessons != nil {
			_spec.Node.AddColumnOnce(quizzes.FieldLessonID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *QuizzesQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(quizzes.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = quizzes.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// QuizzesGroupBy is the group-by builder for Quizzes entities.
type QuizzesGroupBy struct {
	selector
	build *QuizzesQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *QuizzesGroupBy) Aggregate(fns ...AggregateFunc) *QuizzesGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *QuizzesGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*QuizzesQuery, *QuizzesGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *QuizzesGroupBy) sqlScan(ctx context.Context, root *QuizzesQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// QuizzesSelect is the builder for selecting fields of Quizzes entities.
type QuizzesSelect struct {
	*QuizzesQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *QuizzesSelect) Aggregate(fns ...AggregateFunc) *QuizzesSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *QuizzesSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*QuizzesQuery, *QuizzesSelect](ctx, _s.QuizzesQuery, _s, _s.inters, v)
}

func (_s *QuizzesSelect) sqlScan(ctx context.Context, root *QuizzesQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
